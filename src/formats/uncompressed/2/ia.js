import UncompressedData from "../../uncompressed.js";

/**
 *
 *
 * @class IAData
 * @augments {UncompressedData}
 */
class IAData extends UncompressedData {
	static channels = 2;
}

export default IAData;
